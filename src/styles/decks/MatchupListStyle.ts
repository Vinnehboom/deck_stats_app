import { StyleSheet } from "react-native";

import { Typography } from "../variables";

export const MatchupListStyle = StyleSheet.create({
  text: {
    fontSize: Typography.fontSizes.xl,
    fontWeight: "bold",
    textAlign: "center",
  },
});
